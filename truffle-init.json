{
  "ignore": [
    ".travis.yml",
    "docker"
  ],
  "commands": {
    "Compile": "truffle compile",
    "Migrate": "truffle migrate",
    "Test": "truffle test",
    "Build Frontend": "npm run build",
    "Run Linter": "npm run lint",
    "Run Dev Server": "truffle serve"
  },
  "epilogue": "This project includes a web application and user interface, and uses webpack as the build tool to bundle it all together. After compiling and migrating your contracts and running the builder, open ./build/index.html in your browser. The app is configured to integrate directly with MetaMask, so make sure you have that installed and configured before interacting with your application."
}
